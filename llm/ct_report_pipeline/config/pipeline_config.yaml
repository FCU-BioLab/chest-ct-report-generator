# Pipeline Configuration

# Segmentation Model Settings
segmentation_model: "medsam2"

# MedSAM2 Configuration
medsam2:
  root: "C:/GitHub/chest-ct-report-generator/llm/ct_report_pipeline/segmentation/MedSAM2"
  checkpoint: "C:/GitHub/chest-ct-report-generator/llm/ct_report_pipeline/segmentation/MedSAM2_best_model.pth"
  config: "sam2.1_hiera_t512.yaml"

# Dataset Split (for test evaluation)
dataset_split: "C:/GitHub/chest-ct-report-generator/segmentation/result/segmentation_20251215_170725/dataset_split.json"

# LLM Configuration
llm:
  model_name: "meta-llama/Llama-3.2-1B-Instruct"
  lora_path: "assets/models/lora_ct_report/lora_20251220_000011/final"  # Fine-tuned LoRA weights (203 samples)
  load_in_8bit: false  # Small model, no need for quantization
  max_length: 2048
  temperature: 0.3

# Data Paths
lndb_root: "C:/GitHub/chest-ct-report-generator/datasets/aLL_patients_data/LNDb"
output_dir: "C:/GitHub/chest-ct-report-generator/llm/ct_report_pipeline/outputs"
processed_data_dir: "C:/GitHub/chest-ct-report-generator/llm/ct_report_pipeline/processed_data"

# Feature Extraction
encoder_dim: 512
llm_dim: 4096

# Device
device: "cuda"  # or "cpu"

