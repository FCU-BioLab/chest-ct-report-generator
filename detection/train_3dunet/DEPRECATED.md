# Deprecated

`detection.train_3dunet` is no longer the active detection pipeline.

Current project direction:

- Active detection backbone: `detection.retinanet`
- Shared detection utilities: `detection.common`
- This folder is retained only for historical experiments and comparison

Do not use `train_3dunet` for new training runs, documentation, or pipeline integration.
