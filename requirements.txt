# Chest CT Report Generator - Unified Requirements
#
# This file intentionally does NOT install torch / torchvision.
# Install the matching PyTorch build for each machine first.
# Example:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# After PyTorch is ready, install the commented "PyTorch-dependent" section as needed.

# Core scientific stack
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0
scikit-image>=0.21.0

# Medical imaging
SimpleITK>=2.2.0
nibabel>=5.1.0
pydicom>=2.4.0

# Vision / visualization
opencv-python>=4.8.0
Pillow>=10.0.0
matplotlib>=3.7.0
imageio>=2.34.0
albumentations>=1.3.0,<1.4.0

# Config / utilities
PyYAML>=6.0
python-dotenv>=1.0.0
tqdm>=4.65.0
hydra-core>=1.3.2
omegaconf>=2.3.0
iopath>=0.1.10

# Hugging Face / LLM tooling that does not pin torch directly here
transformers>=4.50.0
datasets>=2.14.0
evaluate>=0.4.0
huggingface_hub>=0.25.0
nltk>=3.8.1
ollama>=0.4.0

# GUI / RAG / document tools
PyQt6>=6.7.0
PyMuPDF>=1.24.0
faiss-cpu>=1.8.0

# Optional non-PyTorch extras for specific scripts
ffmpeg-python>=0.2.0
moviepy>=1.0.3

# Test / development helpers
pytest>=7.4.0

# ---------------------------------------------------------------------------
# PyTorch-dependent packages
# Install these only AFTER you manually install the correct torch/torchvision.
# Keeping them commented avoids pip pulling an incompatible CPU/CUDA torch build.
# ---------------------------------------------------------------------------
# accelerate>=0.27.0
# peft>=0.10.0
# bitsandbytes>=0.43.0
# monai>=1.3.0
# segmentation-models-pytorch>=0.3.0
# sentence-transformers>=3.0.0
# lungmask>=0.2.20
# trl>=0.9.0
# pytorch-ignite>=0.4.11
# submitit>=1.5.1
# tensordict>=0.5.0
# eva-decord>=0.6.1
# pycocotools>=2.0.8
# gradio==3.38.0